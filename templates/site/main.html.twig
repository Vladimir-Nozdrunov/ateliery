{% extends 'site/site_base.html.twig' %}


{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-primary" href="{{ path('site_registration') }}">Регистрация</a>
        </div>
        <div class="col-md-6">
            <a class="btn btn-success" href="{{ path('app_login') }}">Вход</a>
        </div>
    </div>
</div>

{% endblock %}